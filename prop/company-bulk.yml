resilience4j:
  bulkhead:
    configs:
      default:
        max-concurrent-calls: 3
        max-wait-duration: 1s
    instances:
      license:
        base-config: default
        max-concurrent-calls: 2