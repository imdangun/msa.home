spring:
  application:
    name: config
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/imdangun/msa.git
          searchPaths: /prop
          clone-on-start: true

server:
  port: 8070