spring:
  application:
    name: config
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/imdangun/msa.prop.25.11.git
          searchPaths: /
          clone-on-start: true

server:
  port: 8070